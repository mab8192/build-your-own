#pragma once

/* ATOMS */
const float MIN_SEPARATION_DIST = 30.0f;
const float SEPARATION_FORCE_STRENGTH = 100.0f;

/* BONDS */
const float BOND_STRENGTH = 10.0f; // should depend on bond type... (TODO)
const float BOND_TARGET_DISTANCE = 30.0f;
const float BOND_BREAK_DISTANCE = 40.0f;
const float BOND_MIN_FORCE = 0.1f;

/* RENDERING */
const float ATOM_RADIUS = 8.0f;